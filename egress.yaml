api_key: devkey
api_secret: devkey
ws_url: ws://livekit:7880

redis:
  address: redis:6379

logging:
  level: info

# Limits
cpu_cost:
  room_composite_cpu_cost: 3.0
  web_cpu_cost: 3.0
  audio_room_composite_cpu_cost: 0.5
